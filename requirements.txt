# =============================================================
# Recipe Preparation Agent — Requirements
# =============================================================
# Install with: pip install -r requirements.txt
# =============================================================

--extra-index-url https://download.pytorch.org/whl/cpu
# Web Framework
Flask==3.0.3
Werkzeug==3.0.3

# Environment Variables
python-dotenv==1.0.1

# HTTP Requests (IBM Watsonx.ai API)
requests==2.32.3

# AI / ML
sentence-transformers==3.0.1
faiss-cpu>=1.8.0
numpy==1.26.4

# IBM Watsonx.ai SDK (optional — direct REST API is also used)
ibm-watsonx-ai==1.1.2

# Data Processing
# (json and os are standard library — no extra install needed)

# Production Server
gunicorn==22.0.0

# Utilities
tqdm==4.66.4
